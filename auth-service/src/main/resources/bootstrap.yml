spring:
  application:
    name: auth-service
  profiles:
    active: mongo-token-store
  cloud:
    config:
      uri: http://localhost:7000
      fail-fast: true
      password: 1234
      username: user
